# Ejemplo mínimo para desplegar una app en NEBULA
version: '3.8'
services:
  my-app:
    image: nginx:alpine
    container_name: my-app
    volumes:
      - ./html:/usr/share/nginx/html
    networks:
      - nebula-network

networks:
  nebula-network:
    external: true
